import React, { Component } from "react";
import MapState from "./MapState";

class App extends Component {
  render() {
    return (
      <div>
        <MapState />
      </div>
    );
  }
}

export default App;
